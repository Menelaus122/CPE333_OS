# KMUTT
**King Mongkut's University of Technology Thonburi**  
Faculty of Engineering, Department of Computer Engineering  
**CPE 333 Operating Systems, 1/2024**

---

## PROBLEM SESSION: Virtual Memory

---

### 1. Understand the concept of virtual memory

1. **Explain and compare how virtual memory is implemented in modern Linux and MS Windows?**
2. **Show step-by-step how to increase or decrease swap space in Linux (with screenshots).**

---

### 2. Understand the Linux VM Monitoring

1. **Run `free` command in the Linux shell.**
2. **Capture the output and explain everything that is shown by this command.**
3. **Summarize all numerical values shown by this command into a paragraph.**
4. **Use `free --help` and select 2 useful arguments of the `free` command** (could be combination of multiple arguments if it is interesting). For each argument:
   - Describe what it does.
   - Show a sample command and explain the output.
5. **If you do not have `vmstat`, install and run it in the Linux shell.**
6. **Capture the output and explain everything that is shown by this command.**
7. **Summarize all numerical values shown by this command into a paragraph.**
8. **Use `vmstat --help` and select 2 useful arguments of the `vmstat` command** (could be combination of multiple arguments if it is interesting). For each argument:
   - Describe what it does.
   - Show a sample command and explain the output.

> **Note:** Please capture the output image from running the command for both `free` and `vmstat`.

---

### 3. Understand Windows VM Monitoring

1. **Open "Resource Monitor" or use command `resmon` in PowerShell.**
2. **Capture the output and explain the information shown on the interface related to memory monitoring.**
3. **Summarize all numerical values shown in the "Physical Memory" tab into a paragraph.**

---

## Submission

- Submit the report containing all the answers for each instruction into **LEB2** before the due date.
- This is a problem session for a group of up to **3–4 students**.